post:
  tags:
  - order-acceptance-webhooks
  summary: Order accepted by restaurant
  description: "This webhook will be invoked whenever the order was accepted by restaurant."
  requestBody:
    required: true
    content:
      application/json:
        schema:
          "$ref": "#/definitions/order-accepted-by-restaurant"
  responses:
    '200':
      description: Respond with a 200 status code to indicate that you have received the notification.
